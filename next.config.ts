/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,
  images: {
    domains: [],
    remotePatterns: [],
  },
};

export default nextConfig;
